[
	// Defines "id" as mandatory module input parameter.
	{
		"name": "id",        // Makes value accesible via "{{parameters.id}}".
		"type": "select",  // Sets the type to unsigned integer.
		"label": "Movie ID",  // Sets the user friendly label visible in the module.
		"required": true,     // Sets the parameter as mandatory.
		"options": "rpc://listMovies?order=desc&sort=name",
		"mode": "edit"
	}
]